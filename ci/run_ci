#! /usr/bin/env nix-shell
#! nix-shell --pure -i bash -p rustup

set -eux

# Unforunately we can't use the nix shell from the 'do', as the version of cargo 
# in nixpkgs does not build the project.
#
# We could revisit this in the future.

rustup install stable
rustup default stable
bash ./do test